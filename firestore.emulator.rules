// E2E 테스트용 에뮬레이터 전용 규칙입니다.
// 운영 Firestore 규칙과는 무관하며, 배포하지 마세요.
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
