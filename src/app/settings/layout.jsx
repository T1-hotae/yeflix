export const metadata = {
  title: '설정',
  description: '계정과 앱 설정을 관리합니다.',
  alternates: { canonical: '/settings' },
  robots: { index: false, follow: false },
};

export default function SettingsLayout({ children }) {
  return children;
}
